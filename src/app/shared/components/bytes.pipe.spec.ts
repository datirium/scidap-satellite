/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BytesPipe } from './bytes.pipe';

describe('Pipe: Bytese', () => {
  it('create an instance', () => {
    let pipe = new BytesPipe();
    expect(pipe).toBeTruthy();
  });
});
